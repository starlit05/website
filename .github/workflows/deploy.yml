on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: deploy2nekoweb
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      # your build step here
        
      - name: deploy2nekoweb
        uses: indiefellas/deploy2nekoweb@main
        with:
          nekoweb-api-key: ${{ secrets.143c63f657a2f1b746bb37f517331859db584bccec140395a83c7bbbc1cc70cb }}
          nekoweb-domain: 'sol.nekoweb.org'
          nekoweb-username: 'sol'
          directory: 'public'
